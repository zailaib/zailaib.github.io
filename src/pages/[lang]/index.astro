---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Home from '../../components/Home.astro';
import { getLatestPosts, getLatestApps } from '../../lib/content';

export function getStaticPaths() {
  return [{ params: { lang: 'zh' } }, { params: { lang: 'en' } }];
}

const { lang } = Astro.params;
const latest = await getLatestPosts(lang!, 6);
const latestApps = await getLatestApps(lang!, 6);
---
<BaseLayout currentSlug="">
  <Home latest={latest} latestApps={latestApps} lang={lang!} />
</BaseLayout>
